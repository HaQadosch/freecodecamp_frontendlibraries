import React from 'react'
import './display.css'

interface IDisplay {

}

export const Display: React.FC<IDisplay> = ({ }) => {
  return (
    <section id="display">
      Display
    </section>
  )
}
